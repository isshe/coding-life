# 第十章 容器化技术

## 问题

- 为什么容器中看到的进程 PID 一半比宿主机小？
- 一个进程只有一个 PID 吗？

## 总结

### 为什么容器中看到的进程 PID 一半比宿主机小？

容器中拥有独立的 PID 命名空间，其 PID 申请也是从小到大的。

### 一个进程只有一个 PID 吗？

`根 PID 命名空间`中的进程只有一个 PID，子孙命名空间中的进程，会有多个 PID —— 当前 PID 命名空间及其所有父 PID 命名空间都申请一个 PID。
